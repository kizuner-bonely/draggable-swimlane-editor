const droppableIdMap = {
  menu: 'menu',
  content: 'content',
}

export default droppableIdMap
export type droppableIdMapKeys = keyof typeof droppableIdMap

const MENU = 'menu'
const CONTENT = 'content'
export {
  MENU,
  CONTENT,
}
